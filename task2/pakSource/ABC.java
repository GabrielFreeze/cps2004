package pakSource;

public class ABC extends Crypto {
    public ABC(String symbol, int totalSupply, int decimals, double exchangeRate) {
        super(symbol, totalSupply, decimals, exchangeRate);
    }
}
